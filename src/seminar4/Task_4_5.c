#include <stdio.h>

int main() {

    int a, b, c, d;

    while ((c = getchar()) != EOF) {
        if (c == ' ')
        {
            printf("\n");
        } else {
            putchar(c);
        }
        
        }
    
    

return 0;   
}